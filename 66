<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <title>調味罐力學模擬 APP</title>
  <style>
    body { font-family: sans-serif; background: #0f172a; color: white; text-align: center; padding: 20px; }
    .card { background: #1e293b; padding: 25px; border-radius: 20px; width: 300px; margin: auto; box-shadow: 0 10px 30px rgba(0,0,0,0.5); border: 1px solid #334155; }
    .bottle-box { height: 200px; display: flex; align-items: flex-end; justify-content: center; margin-bottom: 20px; }
    .bottle { width: 70px; transform-origin: bottom center; transition: transform 0.2s cubic-bezier(0.17, 0.67, 0.83, 0.67); }
    .cap { background: #d2b48c; height: 35px; border-radius: 8px 8px 2px 2px; } /* 米色蓋子 */
    .body-part { background: rgba(255,255,255,0.15); height: 110px; border: 1px solid rgba(255,255,255,0.4); border-radius: 2px 2px 10px 10px; backdrop-filter: blur(4px); } /* 透明罐身 */
    input { width: 100%; margin: 15px 0; accent-color: #d2b48c; }
    .status { padding: 12px; border-radius: 8px; margin-top: 15px; font-weight: bold; }
    .safe { background: #064e3b; color: #6ee7b7; border: 1px solid #059669; }
    .danger { background: #7f1d1d; color: #fca5a5; border: 1px solid #dc2626; }
    .label { font-size: 0.9rem; color: #94a3b8; text-align: left; margin: 0; }
  </style>
</head>
<body>
  <h1>🍶 產品力學穩定度模擬</h1>
  <div class="card">
    <div class="bottle-box">
      <div class="bottle" id="bottle">
        <div class="cap"></div>
        <div class="body-part"></div>
      </div>
    </div>
    <p class="label">重心高度 (Gravity Height): <span id="h-txt">150</span>mm</p>
    <input type="range" min="100" max="400" value="150" id="h-in">
    <p class="label">內容物重量 (Content Weight): <span id="w-txt">3</span>kg</p>
    <input type="range" min="1" max="10" value="3" id="w-in">
    <div id="msg" class="status safe">✔ 結構重心穩定</div>
  </div>
  <script>
    const hIn = document.getElementById('h-in'), wIn = document.getElementById('w-in');
    const b = document.getElementById('bottle'), m = document.getElementById('msg');
    function validate() {
      let h = hIn.value, w = wIn.value;
      document.getElementById('h-txt').innerText = h;
      document.getElementById('w-txt').innerText = w;
      let risk = h / w; 
      b.style.transform = `rotate(${(risk-50)*0.2}deg)`;
      if(risk > 100) { 
        m.innerText = "⚠️ 警報：力矩偏離安全區"; 
        m.className = "status danger"; 
      } else { 
        m.innerText = "✔ 結構重心穩定"; 
        m.className = "status safe"; 
      }
    }
    hIn.oninput = validate; wIn.oninput = validate;
  </script>
</body>
</html>
